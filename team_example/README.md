# Лабораторная работа: Система управления умным домом (Smart Home System)

## Описание

Проект демонстрирует реализацию системы управления устройствами умного дома с использованием паттернов проектирования **Factory Pattern** и **Polymorphism** на C++.

Система состоит из:
- **Базовый класс Device** — определяет интерфейс для всех устройств
- **Конкретные устройства** (Lamp, HairDryer) — наследуют от Device и реализуют собственную логику
- **DeviceFactory** — фабрика для создания устройств
- **DeviceController** — управляет коллекцией устройств и организует их работу
- **Симуляция дня** — каждое устройство имеет сценарий типичного дневного использования

## Структура проекта

```
team_example/
├── device.h / device.cpp              # Базовый класс для всех устройств
├── concrete_device_1.h / concrete_device_1.cpp  # Реализации Lamp и HairDryer
├── device_factory.h / device_factory.cpp        # Фабрика для создания устройств
├── device_controller.h / device_controller.cpp  # Контроллер управления устройствами
├── main.cpp                           # Демонстрационная программа
└── README.md                          # Этот файл
```

## Ключевые концепции

### 1. Паттерн Factory
```cpp
// Создание устройств без знания их конкретного типа
std::unique_ptr<Device> lamp = DeviceFactory::createDeviceByName("Lamp");
std::unique_ptr<Device> dryer = DeviceFactory::createDeviceByName("HairDryer");
```

### 2. Полиморфизм
```cpp
// DeviceController работает с указателями на базовый класс Device
// но вызывает методы конкретных реализаций
for (auto& device : devices) {
    device->simulateDay();  // Вызовется метод конкретного класса
}
```

### 3. Умные указатели (std::unique_ptr)
```cpp
std::vector<std::unique_ptr<Device>> devices;
// Автоматическое управление памятью — объекты удаляются при выходе из области видимости
```

## Текущая реализация

### Устройства

#### Лампа (Lamp)
- Максимальный заряд: 50 Вт·ч
- Потребление: 10 Вт
- Сценарий дня:
  - Утром: включение → 3 часа работы → выключение
  - Вечером: включение → 4 часа работы → выключение

#### Фен (HairDryer)
- Максимальный заряд: 80 Вт·ч
- Потребление: 1200 Вт
- Сценарий дня:
  - Утром: включение → 30 минут работы → выключение
  - Вечером: включение → 10 минут работы → выключение

## Задачи для студентов

### Уровень 1: Базовые

1. **Создать новое устройство**
   - Создайте класс `CoffeeMaker`, наследующий от `Device`
   - Реализуйте методы `getDescription()`, `printStatus()` и `simulateDay()`
   - Добавьте в отдельный файл `coffee_maker.h` и `coffee_maker.cpp`

2. **Зарегистрировать устройство в фабрике**
   - Добавьте `CoffeeMaker` в `DeviceFactory::createDeviceByName()`
   - Обновите `createRandomDevice()` для поддержки нового типа

3. **Продемонстрировать в main**
   - Создайте экземпляры новых устройств через фабрику
   - Добавьте их в контроллер
   - Выведите описание и статус устройств

### Уровень 2: Средний

4. **Усложнить логику simulateDay()**
   - Добавьте несколько циклов включения/выключения (более реалистичный сценарий)
   - Выводите детальные сообщения о каждом действии с временными метками

5. **Добавить дополнительные методы в Device**
   - Например, `getEnergyConsumption()` — расчет потребления за день
   - Или `needsMaintenance()` — проверка необходимости обслуживания

6. **Исправить логику подзарядки устройств**
   - Модифицируйте `chargeDevicesThatNeedIt()` в контроллере
   - Реализуйте собственную стратегию подзарядки: например, заряжайте устройство, если заряд упал ниже 50% (вместо 20%)
   - Или добавьте приоритеты: критичные устройства заряжаются в первую очередь
   - Выводите информацию о том, какие устройства были подзаряжены и почему

7. **Добавить уникальные имена устройствам**
   - Расширьте класс `Device` с полем `uniqueName` (например, "Лампа в спальне", "Фен №1")
   - Обновите конструктор: `Device(const std::string& name, const std::string& uniqueName, ...)`
   - Выводите в консоль `uniqueName` вместо просто `name`
   - В `main` при создании устройств передавайте оригинальное описание и уникальное имя:
     ```cpp
     controller.addDevice(std::make_unique<Lamp>("Лампа", "Лампа в спальне"));
     controller.addDevice(std::make_unique<HairDryer>("Фен", "Фен в ванной №1"));
     ```

8. **Расширить DeviceController**
   - Добавьте метод `findDeviceByName(name)` для поиска устройства по уникальному имени
   - Реализуйте сценарий расписания (устройства включаются/выключаются по времени)

### Уровень 3: Продвинутый

9. **Система энергоснабжения**
   - Реализуйте расчет потребленной энергии за день
   - Добавьте стоимость электроэнергии
   - Создайте энергетический отчет для каждого устройства

10. **Интерактивный режим**
    - Добавьте меню для управления устройствами из консоли
    - Команды: "включить", "выключить", "зарядить", "статус"
    - Поиск устройства по уникальному имени

11. **Сохранение и загрузка конфигурации**
    - Реализуйте сохранение списка устройств в файл
    - Загружайте конфигурацию при запуске программы
    - Сохраняйте уникальные имена и состояние устройств


## Примеры устройств для реализации

Если вам не хватает фантазии, вот готовые идеи устройств, которые вы можете реализовать:

| Устройство | Потребление (Вт/час) | Батарея (Вт·ч) | Примечание |
|-----------|-------------------|---------------|-----------|
| Умная лампочка | 8 | 96 | Альтернатива обычной лампе |
| Робот-пылесос | 45 | 225 | Автоматическая уборка |
| Умный термостат | 3 | 72 | Минимальное потребление |
| Умная кофеварка | 900 (вкл) / 50 (ожидание) | 500 | Высокое потребление при работе |
| Холодильник с сенсорами | 100 (компрессор) / 5 (ожидание) | 2000 | Большая батарея, критичное устройство |
| Умная духовка | 2000 (работа) / 30 (ожидание) | 300 | Требует частой зарядки |
| Умный телевизор | 80 (просмотр) / 1 (ожидание) | 400 | Длительное использование |
| Система умных штор | 25 (движение) / 0.5 (ожидание) | 150 | Редкое использование |
| Умная колонка | 15 (активная) / 3 (ожидание) | 120 | Фоновое устройство |
| Умная кровать | 10 | 240 | Подогрев, массаж |
| Умное зеркало | 40 (включено) / 5 (ожидание) | 180 | Дисплей + освещение |
| Умный дозатор | 2 | 48 | Очень экономичное |
| Умный вентилятор | 30 (работа) / 5 (ожидание) | 180 | Летний помощник |
| Умный чайник | 1500 (кипячение) / 20 (ожидание) | 600 | Кратковременное высокое потребление |
| Умный проектор | 120 (проекция) / 2 (ожидание) | 480 | Развлечение |

### Как выбрать устройства для реализации?

**Рекомендуемые комбинации:**

- **Минимальный набор:** Термостат + Колонка + Лампа (разные потребления)
- **Средний набор:** Кофеварка + Робот-пылесос + Чайник + Лампа
- **Полный набор:** Выберите 4-5 устройств с разными характеристиками

### Советы по реализации новых устройств:

1. **Копируйте структуру** из `concrete_device_1.cpp` — создавайте конструктор, `getDescription()`, `printStatus()`, `simulateDay()`
2. **Думайте о сценариях** — как это устройство используется в реальной жизни?
   - Кофеварка: утром короткая работа, вечером может быть еще раз
   - Холодильник: постоянный фон, редко выключается
   - Телевизор: длительные сеансы просмотра вечером
3. **Разнообразьте сценарии** — не делайте все устройства с одинаковым количеством циклов вкл/выкл

## Примеры использования

### Создание и запуск системы

```cpp
#include "device_factory.h"
#include "device_controller.h"

int main() {
    // Инициализация UTF-8 для вывода кириллицы
    SetConsoleCP(65001);
    SetConsoleOutputCP(65001);

    // Создаем контроллер
    DeviceController controller;

    // Добавляем устройства через фабрику
    controller.addDevice(DeviceFactory::createDeviceByName("Lamp"));
    controller.addDevice(DeviceFactory::createDeviceByName("HairDryer"));
    controller.addDevice(DeviceFactory::createDeviceByName("CoffeeMaker")); // Новое!

    // Выводим описание
    controller.printDevicesDescription();

    // Заряжаем все устройства
    controller.chargeDevicesThatNeedIt();

    // Симулируем три дня
    for (int day = 1; day <= 3; ++day) {
        controller.simulateDayOfWork(day);
        controller.printAllDevicesStatus();
        controller.chargeDevicesThatNeedIt();
    }

    // Итоговый отчет
    controller.printEnergyReport();

    return 0;
}
```

## Компиляция и запуск

### На Linux/macOS:
```bash
g++ -std=c++17 -Wall -Wextra device.cpp lamp_device.cpp device_factory.cpp device_controller.cpp main.cpp -o smart_home
./smart_home
```

### На Windows (MinGW):
```bash
cl -std=c++17 -Wall -Wextra device.cpp lamp_device.cpp device_factory.cpp device_controller.cpp main.cpp -o smart_home.exe
smart_home.exe
```

## Учебные цели

После выполнения этой работы студенты научатся:

- ✅ Применять паттерны проектирования (Factory, Polymorphism)
- ✅ Работать с наследованием и виртуальными методами
- ✅ Использовать контейнеры STL (vector)
- ✅ Управлять памятью с помощью умных указателей (unique_ptr)
- ✅ Организовывать код в отдельные файлы (.h/.cpp)
- ✅ Работать с объектно-ориентированным дизайном

## Советы по разработке

1. **Начните с простого** — сначала создайте устройство с минимальной логикой
2. **Тестируйте постепенно** — проверяйте каждый новый класс перед добавлением в систему
3. **Следуйте соглашениям** — используйте консистентные имена методов и переменных
4. **Документируйте код** — добавляйте комментарии для сложных мест
5. **Используйте git** — сохраняйте все промежуточные версии

## Расширения и модификации

### Идеи для расширения:
- Система приоритетов устройств (критичные vs некритичные)
- Расписание работы устройств (время включения/выключения)
- Система уведомлений при низком заряде
- Группировка устройств по комнатам
- Статистика использования энергии

**Успехов в разработке! 🚀**
