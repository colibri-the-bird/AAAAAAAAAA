// lamp_device.cpp
// Реализация конкретных устройств
#include "devices.h"
#include <iostream>
#include <algorithm>

// ===== ЛАМПА =====
Lamp::Lamp() : Device("Лампа", 100.0, 10.0) {}

std::string Lamp::getDescription() const {
    return "Лампа (потребление: 10 Вт)";
}

void Lamp::printStatus() const {
    std::cout << "💡 ";
    Device::printStatus();
}

void Lamp::simulateDay() {
    // Симуляция типичного дня для лампы — показываем включения/выключения и время работы
    std::cout << "💡 [" << name << "] — симуляция дня." << std::endl;

    // Утреннее использование
    std::cout << "  Использование лампы утром: 3 часа." << std::endl;
    if (!isRunning) {
        turnOn();
    }
    work(3.0);
    // Лампа выключается после утреннего блока
    turnOff();

    // Вечернее использование
    std::cout << "  Использование лампы вечером: 4 часа." << std::endl;
    turnOn();
    work(4.0);
    turnOff();
}

// ===== CoffeeMaker =====
CoffeeMaker::CoffeeMaker() : Device("Лампа", 100.0, 10.0) {}

std::string CoffeeMaker::getDescription() const {
    return "Лампа (потребление: 10 Вт)";
}

void CoffeeMaker::printStatus() const {
    std::cout << "💡 ";
    Device::printStatus();
}

void CoffeeMaker::simulateDay() {
    // Симуляция типичного дня для лампы — показываем включения/выключения и время работы
    std::cout << "💡 [" << name << "] — симуляция дня." << std::endl;

    // Утреннее использование
    std::cout << "  Использование лампы утром: 3 часа." << std::endl;
    if (!isRunning) {
        turnOn();
    }
    work(3.0);
    // Лампа выключается после утреннего блока
    turnOff();

    // Вечернее использование
    std::cout << "  Использование лампы вечером: 4 часа." << std::endl;
    turnOn();
    work(4.0);
    turnOff();
}


// ===== Робот-пылесос =====
VacuumBot::VacuumBot() : Device("Лампа", 100.0, 10.0) {}

std::string VacuumBot::getDescription() const {
    return "Лампа (потребление: 10 Вт)";
}

void VacuumBot::printStatus() const {
    std::cout << "💡 ";
    Device::printStatus();
}

void VacuumBot::simulateDay() {
    // Симуляция типичного дня для лампы — показываем включения/выключения и время работы
    std::cout << "💡 [" << name << "] — симуляция дня." << std::endl;

    // Утреннее использование
    std::cout << "  Использование лампы утром: 3 часа." << std::endl;
    if (!isRunning) {
        turnOn();
    }
    work(3.0);
    // Лампа выключается после утреннего блока
    turnOff();

    // Вечернее использование
    std::cout << "  Использование лампы вечером: 4 часа." << std::endl;
    turnOn();
    work(4.0);
    turnOff();
}